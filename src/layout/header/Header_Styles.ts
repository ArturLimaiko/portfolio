import styled from "styled-components";

const Header = styled.header`
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  min-width: 360px;
  background-color: rgba(25, 25 ,25 , 0.8);
`

export const S = {
    Header,
}